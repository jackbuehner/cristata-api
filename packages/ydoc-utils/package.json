{
  "name": "@jackbuehner/cristata-ydoc-utils",
  "version": "3.0.3",
  "author": "Jack Buehner",
  "license": "ISC",
  "main": "dist/index",
  "scripts": {
    "dev": "tsc && tsc --watch",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "clean": "rm -rf dist",
    "build": "tsc --build && cross-env NODE_ENV=production npm run lint",
    "test": "jest"
  },
  "dependencies": {
    "jsdom": "^20.0.0",
    "merge-anything": "^5.0.4"
  },
  "devDependencies": {
    "@types/jsdom": "^20.0.0"
  }
}
